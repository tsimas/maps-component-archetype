angular.module('${angular-name}')
    .controller(
    '${angular-name}Ctrl',
    ['$scope', '$http', '$log', '$filter', '${angular-name}Services',
        function ($scope, $http, $log, $filter, ${angular-name}Services) {

        }]);
        